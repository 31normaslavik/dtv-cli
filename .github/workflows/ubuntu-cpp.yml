name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: install depedensity
      run: sudo apt-get install cmake gcc g++ git
    - name: cmake configure
      run: cmake -S . -B _build
    - name: cmake build
      run: cmake --build _build -j8    
